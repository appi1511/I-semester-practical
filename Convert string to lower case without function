#include<stdio.h>
int main()
{
    char s[100];
    fgets(s,100,stdin);
    for(int i=0;i!='\n';i++)
    {
        if(s[i]>='A'&& s[i]<='Z')
        s[i]=s[i]+32;
    }
    printf("%s",s);
    return 0;
}
