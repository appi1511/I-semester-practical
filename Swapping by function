#include <stdio.h>
void swap(int *x, int*y)
{
    int t=*x;
    *x=*y;
    *y=t;
}
int main()
{
    int a,b;
    scanf("%i %i",&a,&b);
    swap(&a,&b);
    printf("A is %i and B is %i",a,b);

    return 0;
}
