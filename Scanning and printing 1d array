#include<stdio.h>
void scan(int a[],int n)
{
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
}
void print(int a[],int n)
{
    for(int i=0;i<n;i++)
    printf("%d ",a[i]);
}
int main()
{
    int a[10],b[10],c[10],n;
    scanf("%i",&n);
    scan(b,n);
    print(b,n);
    return 0;
}
