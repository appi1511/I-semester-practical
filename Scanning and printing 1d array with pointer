#include<stdio.h>
void scan(int b[],int q)
{
    for (int i=0;i<q;i++)
    scanf("%i",&b[i]);
}
int print(int b[],int q)
{
    for(int i=0;i<q;i++)
    printf("%i ",b[i]);
}
int main()
{
    int n;
    scanf("%d",&n);
    int *p=&n;
    int a[n];
    scan(a,*p);
    print(a,*p);
    return 0;
}
